#include <iostream>
using namespace std;
int main() {
    int a,b,t;
    cout<<"Enter First Number : ";
    cin>>a;
    cout<<"Enter Second Number : ";
    cin>>b;
    t = a + b;
    cout<< "Sum of "<<a<<" and "<<b<<" is "<<t;

    return 0;
}
